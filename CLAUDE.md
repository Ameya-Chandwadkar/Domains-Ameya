# Scout Regiment Hackathon

Static site: plain HTML5, CSS3, and vanilla JavaScript (ES6). No build step,
no framework, no bundler. Open `index.html` directly or serve the folder
statically. See `README.md` for file structure.
