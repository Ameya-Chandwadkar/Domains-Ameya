"use client";
import { AnimatePresence, motion } from "framer-motion";
import type { Domain } from "./domains";

export function DomainInfoPanel({ domain, onClose }: { domain: Domain | null; onClose: () => void }) { return <AnimatePresence>{domain && <motion.aside role="dialog" aria-label={`${domain.title} details`} initial={{ opacity: 0, x: 32, y: 12 }} animate={{ opacity: 1, x: 0, y: 0 }} exit={{ opacity: 0, x: 32, y: 12 }} transition={{ type: "spring", damping: 22, stiffness: 185 }} className="absolute inset-x-3 bottom-4 z-30 border border-stone-700/50 bg-[#1a1512]/95 p-5 shadow-[0_0_45px_rgba(0,0,0,.6)] backdrop-blur-md md:bottom-8 md:left-auto md:right-8 md:w-[26rem]"><div className="absolute inset-x-0 top-0 h-px bg-red-900/80" /><div className="flex items-start justify-between gap-4"><div><p className="font-mono text-[10px] tracking-[.22em] text-stone-400">MISSION DOSSIER / {domain.character.toUpperCase()}</p><h2 className="mt-2 text-3xl font-black uppercase tracking-tight text-[#f2e6d9]">{domain.title}</h2></div><button onClick={onClose} className="rounded border border-stone-500/20 px-2 py-1 font-mono text-xs text-[#f2e6d9]/75 hover:bg-white/10">ESC</button></div><p className="mt-3 text-sm leading-6 text-stone-300/80">{domain.description}</p><div className="mt-4 grid grid-cols-2 gap-4 border-y border-stone-600/20 py-4 text-xs"><div><p className="font-mono text-[9px] tracking-widest text-stone-400">PROJECT IDEAS</p>{domain.ideas.map((idea) => <p key={idea} className="mt-1 text-[#f2e6d9]/85">{idea}</p>)}</div><div><p className="font-mono text-[9px] tracking-widest text-stone-400">TECH STACK</p><p className="mt-1 leading-5 text-[#f2e6d9]/85">{domain.technologies.join(" · ")}</p></div></div><div className="mt-4 flex justify-between gap-3 font-mono text-[10px] text-[#f2e6d9]/65"><span>LEVEL: <b className="text-stone-300">{domain.difficulty}</b></span><span>{domain.team}</span></div><a href={`#explore-${domain.id}`} className="mt-5 flex justify-center border border-red-900/50 bg-red-900/10 px-4 py-3 text-xs font-bold uppercase tracking-[.16em] text-red-50 transition hover:bg-red-900 hover:text-white">Explore domain →</a></motion.aside>}</AnimatePresence>; }
